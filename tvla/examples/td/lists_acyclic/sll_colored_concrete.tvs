// An acyclic singly-linked list (where the colors alternate) with two or more elements pointed by program x.
%n = {head, mid, tail}
%p = {
       n = {head->mid, mid->tail}
       x = {head}
       t[n] = {head->head, head->mid, tail->tail, mid->mid, mid->tail, head->tail}
       r[n,x] = {head, mid, tail}
       color = {mid}
       altlist[n] = {head, mid, tail}
       last_elem = {tail}
}
